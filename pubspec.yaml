name: bico_certo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  # Armazenar dados simples de forma persistente no dispositivo. Dados que ficam salvos mesmo depois que o app é fechado ou o celular é reiniciado.
  shared_preferences: ^2.2.3 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/